class Solution:
    def convertToTitle(self, columnNumber: int) -> str:
        # generate letters sequence (ascii codes from 65 to 90 is a english alphabet in upper case)
        letters = [chr(i) for i in range(65,91)]





a = Solution()
a.convertToTitle(28) #BF